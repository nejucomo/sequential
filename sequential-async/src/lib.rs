#![deny(unsafe_code, unused, missing_docs)]
//! The `SequentialAsync` (TODO) trait and supporting types for abstract asynchronous sequential processing over inputs, outputs, and explicit termination
//!
//! # Related Crates
//! - [sequential] - the synchronous equivalent to this crate.
//! - `seqgen-async` - provides a useful functional subset trait which takes no inputs.
